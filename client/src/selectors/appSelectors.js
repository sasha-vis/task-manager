export const selectLoading = ({ app }) => app.isLoading;
export const selectNotification = ({ app }) => app.notification;
export const selectModal = ({ app }) => app.modal;
export const selectAuthentication = ({ app }) => app.isAuthenticated;
export const selectAuthChecking = ({ app }) => app.isAuthChecking;
